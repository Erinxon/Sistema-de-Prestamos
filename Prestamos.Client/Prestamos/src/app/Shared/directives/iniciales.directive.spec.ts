import { InicialesDirective } from './iniciales.directive';

describe('InicialesDirective', () => {
  it('should create an instance', () => {
    const directive = new InicialesDirective();
    expect(directive).toBeTruthy();
  });
});
