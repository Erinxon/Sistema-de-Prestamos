import { ColorFilaEstatusPrestamoDirective } from './color-fila-estatus-prestamo.directive';

describe('ColorFilaEstatusPrestamoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorFilaEstatusPrestamoDirective();
    expect(directive).toBeTruthy();
  });
});
