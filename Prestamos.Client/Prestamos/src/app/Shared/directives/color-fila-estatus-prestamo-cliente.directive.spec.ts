import { ColorFilaEstatusPrestamoClienteDirective } from './color-fila-estatus-prestamo-cliente.directive';

describe('ColorFilaEstatusPrestamoClienteDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorFilaEstatusPrestamoClienteDirective();
    expect(directive).toBeTruthy();
  });
});
