import { EstatusPrestamoToStringPipe } from './estatus-prestamo-to-string.pipe';

describe('EstatusPrestamoToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new EstatusPrestamoToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
