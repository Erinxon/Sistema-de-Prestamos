import { RolToStringPipe } from './rol-to-string.pipe';

describe('RolToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new RolToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
